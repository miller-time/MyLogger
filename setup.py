from setuptools import setup,find_packages

setup(
    name="MyLogger",
    version="0.6",
    description="Wrapper for logging library",
    author="Russell Miller",
    packages=find_packages(),
    )
